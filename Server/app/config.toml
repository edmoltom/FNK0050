[vision]
enable = true
stream_interval = 0.2
profile = "object"
threshold = 0.5
model_path = "models/default.pt"
log = true     # vision-specific logging

[movement]
enable = true
log = false

[logging]
level = "INFO" # global log level
